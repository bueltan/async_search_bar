name: async_search_bar
description:  A SearchBar widget automatizing most of your asynchronous searchs cases (based on flapy_search_bar).

version: 2.0.0

environment:
  sdk: '>=2.18.4 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  async: ^2.9.0
  flutter_staggered_grid_view: ^0.6.2
  
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
